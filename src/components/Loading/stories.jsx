import { Loading } from '.';

export default {
    title: 'Loading',
    component: Loading,
};

export const Template = () => {
    return (
        <div>
            <Loading />
        </div>
    );
};
